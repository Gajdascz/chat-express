<nav>
  <div class="full-nav-container">
    <h1 class='nav-header'>
      <a href='/' class='nav-header-link'>ChatExpress</a>
    </h1>
    <div class='nav-menu'>
      {{#if currentUser}}
        {{> dropdown 
          idPrefix='nav-' 
          toggleButtonContent=(getAvatarThumb currentUser.avatar.thumb)
          options=(getLoggedInNavOptions currentUser.url)
        }}
      {{else}}
        <div class='nav-action-links-wrapper'>
          <a class='nav-link' href='/user/register'>Register</a>
          <a class='nav-link' href='/user/login'>Login</a>
        </div>
      {{/if}}
    </div>
  </div>
    <div class="mobile-nav-container">
      {{#if currentUser}}
        {{> dropdown 
          idPrefix='nav-' 
          toggleButtonContent=(getAvatarThumb currentUser.avatar.thumb)
          options=(getLoggedInMobileNavOptions currentUser.url)
        }}
      {{else}}
        {{> dropdown idPrefix='nav-' toggleButtonContent='≡' options=(getDefaultMobileNavOptions)}}
      {{/if}}
  </div>
</nav>

